---
phase: 00-architecture-spike-repo-setup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/research/STACK.md
autonomous: true
requirements: [REPO-02]

must_haves:
  truths:
    - "REQ-REPO-02 in REQUIREMENTS.md names the modules `gen` and `rt` (not `scimgen` and `scimrt`)"
    - "REQ-CLI-01 in REQUIREMENTS.md names the CLI binary `scim` (not `scimgen`); the underlying library APIs the CLI wraps still come from the `gen` module"
    - "All references to `scimgen` and `scimrt` across REQUIREMENTS.md, ROADMAP.md, and .planning/research/STACK.md are reconciled to `gen` and `rt`"
    - "Go version floor in .planning/research/STACK.md says `1.25` (not `1.24`); the matrix description matches CONTEXT.md (1.25 + 1.26)"
    - "Each reconciliation has an inline reconciliation note explaining the override (e.g., `(updated 2026-05-07 per Phase 0 CONTEXT.md decision)`) so future readers see the trail"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled REQ-REPO-02 + REQ-CLI-01 wording (and any other scimgen/scimrt references)"
      contains: "gen"
    - path: ".planning/ROADMAP.md"
      provides: "Reconciled phase descriptions (Phase 0 success criteria, Phase 7 CLI references)"
      contains: "gen"
    - path: ".planning/research/STACK.md"
      provides: "Reconciled Go version floor (1.25)"
      contains: "1.25"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/00-architecture-spike-repo-setup/00-CONTEXT.md"
      via: "reconciliation note cites the CONTEXT.md decision"
      pattern: "CONTEXT\\.md|Phase 0"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "ROADMAP phase descriptions reference the requirement IDs whose wording was reconciled"
      pattern: "REPO-02|CLI-01"
---

<objective>
Reconcile the planning documents (REQUIREMENTS.md, ROADMAP.md, .planning/research/STACK.md) with the user decisions captured in CONTEXT.md `<requirements_overrides>`. Plans 01-03 use the new names verbatim; Plan 04's job is to update the canonical written-down requirements so that future agents reading REQUIREMENTS.md don't re-introduce the old `scimgen`/`scimrt` naming.

Purpose: CONTEXT.md `<requirements_overrides>` explicitly flags three deviations the planner must reconcile:
1. Module names: `scimgen` + `scimrt` → `gen` + `rt`
2. CLI binary: `scimgen` → `scim`
3. Go version floor: 1.24 → 1.25

Without this reconciliation, the next phase's planner reads REQUIREMENTS.md, sees `scimgen`/`scimrt`, and a contradiction enters the workflow. Surgical edits only — do not rewrite requirement text beyond what the override demands; do not retroactively change requirement IDs.

Output: Three updated planning documents with override reconciliations and inline notes so the audit trail is preserved.
</objective>

<execution_context>
@/Users/imulab/.claude/get-shit-done/workflows/execute-plan.md
@/Users/imulab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-architecture-spike-repo-setup/00-CONTEXT.md
@.planning/phases/00-architecture-spike-repo-setup/00-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md

<interfaces>
<!-- Override sources (verbatim from CONTEXT.md `<requirements_overrides>`): -->

| Override | From | To | Where it appears |
|----------|------|-----|------------------|
| Module names | `scimgen` + `scimrt` | `gen` + `rt` | REQUIREMENTS.md REQ-REPO-02, REQ-PER-04, REQ-PATCH-08, REQ-LIB-04, REQ-OBS-01; ROADMAP.md Phase 0 success #3, Phase 1 success #5, Phase 3 success #5, Phase 5 success #3, Phase 7 success #3, Phase 7 description |
| CLI binary | `scimgen` | `scim` | REQUIREMENTS.md REQ-CLI-01..04; ROADMAP.md Phase 1 success #3, Phase 7 description, Phase 7 success #1 |
| Go version floor | 1.24 | 1.25 | .planning/research/STACK.md lines ~14, 44, 77, 194 (all `1.24` references where context implies the project floor — does NOT include historical references like "Released Feb 2025" or version commentary) |

<!-- Concrete reference list (built from grep at planning time): -->

REQUIREMENTS.md occurrences of scimgen/scimrt to update:
- Line 36: REQ-PER-04 — `scimrt/sqldriver` → `rt/sqldriver`
- Line 82: REQ-PATCH-08 — `scimrt/patch` → `rt/patch`
- Line 108: REQ-CLI-01 — `scimgen` → `scim` (the CLI binary; library is still in gen module)
- Line 109: REQ-CLI-02 — `scimgen generate` → `scim generate`
- Line 110: REQ-CLI-03 — `scimgen check` → `scim check`
- Line 111: REQ-CLI-04 — `scimgen dump-ir` → `scim dump-ir`
- Line 119: REQ-LIB-04 — `scimrt` runtime module → `rt` runtime module; `scimgen` generator module → `gen` generator module
- Line 123: REQ-OBS-01 — `scimrt/observe` → `rt/observe`
- Line 139: REQ-REPO-02 — `scimgen` (generator) and `scimrt` (runtime support library) → `gen` (generator) and `rt` (runtime support library)

ROADMAP.md occurrences:
- Line 22: Phase 7 description — `scimgen CLI` → `scim CLI`
- Line 35: Phase 0 success #3 — Two `go.mod` modules exist (`scimgen` and `scimrt`) → (`gen` and `rt`)
- Line 47: Phase 1 success #3 — `scimgen dump-ir` → `scim dump-ir`
- Line 49: Phase 1 success #5 — Generator (`scimgen`) and runtime support library (`scimrt`) → (`gen`) and (`rt`); also `scimgen` never appears → `gen` never appears
- Line 73: Phase 3 success #5 — `scimrt/sqldriver` → `rt/sqldriver`
- Line 95: Phase 5 success #3 — `scimrt/patch` → `rt/patch`
- Line 116: Phase 7 success #1 — `scimgen` CLI → `scim` CLI; `scimgen generate` → `scim generate`; `scimgen check` → `scim check`
- Line 118: Phase 7 success #3 — `scimrt/observe` → `rt/observe`

.planning/research/STACK.md occurrences (Go version):
- Line 14: "Go 1.24+ (use 1.25 if available)" → "Go 1.25+ (CI tests 1.25 + 1.26)"
- Line 44: "1.24.x (1.25 once available)" → "1.25.x (CI tests 1.25 + 1.26)"
- Line 77: "Go 1.24+" → "Go 1.25+"
- Line 194: `go 1.24` (in a code-block example) — leave the example illustrative if it makes sense, OR update to `go 1.25` for consistency. Decision: update to 1.25 for cross-document consistency.

<!-- Reconciliation note format (apply once per file's first edit, NOT per line): -->

Add a short reconciliation note in the file header / "Last updated" line:
```
*Updated 2026-05-07 — Phase 0 CONTEXT.md overrides applied: module names scimgen→gen, scimrt→rt; CLI binary scimgen→scim; Go floor 1.24→1.25.*
```

Place this AFTER the existing "Last updated" footer if one exists, or as a new italic line at the top of the document if not.

DO NOT change requirement IDs. DO NOT change requirement intent. DO NOT add or remove requirements. Surgical wording-only changes per CLAUDE.md guideline 3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Update both files in lockstep — they share the `scimgen`/`scimrt` and `scimgen` (CLI) → `scim` overrides. Use `Read` first on each file (mandatory for `Write` if file already exists, or use `Edit` for surgical changes — `Edit` is preferred here since changes are line-level).

Apply the following edits to `.planning/REQUIREMENTS.md`:

1. Line 36 (REQ-PER-04): replace `scimrt/sqldriver` with `rt/sqldriver`.
2. Line 82 (REQ-PATCH-08): replace `scimrt/patch` with `rt/patch`.
3. Line 108 (REQ-CLI-01): replace `\`scimgen\` CLI built on Cobra` with `\`scim\` CLI built on Cobra` (keep the rest of the sentence intact).
4. Line 109 (REQ-CLI-02): replace `\`scimgen generate\`` with `\`scim generate\``.
5. Line 110 (REQ-CLI-03): replace `\`scimgen check\`` with `\`scim check\``.
6. Line 111 (REQ-CLI-04): replace `\`scimgen dump-ir\`` with `\`scim dump-ir\``.
7. Line 119 (REQ-LIB-04): replace `\`scimrt\` runtime module — never the \`scimgen\` generator module` with `\`rt\` runtime module — never the \`gen\` generator module`.
8. Line 123 (REQ-OBS-01): replace `\`scimrt/observe\`` with `\`rt/observe\``.
9. Line 139 (REQ-REPO-02): replace `Two \`go.mod\` modules: \`scimgen\` (generator) and \`scimrt\` (runtime support library)` with `Two \`go.mod\` modules: \`gen\` (generator at \`github.com/imulab/go-scim/gen\`) and \`rt\` (runtime support library at \`github.com/imulab/go-scim/rt\`)`.
10. Append at the end of the file (after the existing `*Last updated: 2026-05-07 — traceability populated by roadmapper*` line) a new italic line:
    `*Updated 2026-05-07 (Phase 0 plan) — CONTEXT.md overrides applied: module names scimgen→gen, scimrt→rt; CLI binary scimgen→scim. Requirement IDs unchanged.*`

Apply the following edits to `.planning/ROADMAP.md`:

1. Line 22 (Phase 7 entry): replace `scimgen CLI` with `scim CLI`.
2. Line 35 (Phase 0 success criterion 3): replace `(\`scimgen\` and \`scimrt\`)` with `(\`gen\` and \`rt\`)`.
3. Line 47 (Phase 1 success criterion 3): replace `\`scimgen dump-ir\`` with `\`scim dump-ir\``.
4. Line 49 (Phase 1 success criterion 5): replace `Generator (\`scimgen\`) and runtime support library (\`scimrt\`) are separately versioned modules; \`scimgen\` never appears` with `Generator (\`gen\`) and runtime support library (\`rt\`) are separately versioned modules; \`gen\` never appears`.
5. Line 73 (Phase 3 success criterion 5): replace `\`scimrt/sqldriver\`` with `\`rt/sqldriver\``.
6. Line 95 (Phase 5 success criterion 3): replace `\`scimrt/patch\`` with `\`rt/patch\``.
7. Line 116 (Phase 7 success criterion 1): replace `\`scimgen\` CLI built on Cobra exposes \`generate\`, \`check\`, and \`dump-ir\`` with `\`scim\` CLI built on Cobra exposes \`generate\`, \`check\`, and \`dump-ir\``; AND replace `\`scimgen generate\`` with `\`scim generate\``; AND replace `\`scimgen check\`` with `\`scim check\``.
8. Line 118 (Phase 7 success criterion 3): replace `\`scimrt/observe\`` with `\`rt/observe\``.
9. (No new footer line needed for ROADMAP.md — it doesn't have a "Last updated" line in the same shape; do NOT add one. Keep ROADMAP edits surgical.)

After all edits, verify:
- `grep -n 'scimgen\|scimrt' .planning/REQUIREMENTS.md` returns ZERO lines.
- `grep -n 'scimgen\|scimrt' .planning/ROADMAP.md` returns ZERO lines.
- `grep -c '^- \[ \] \*\*REQ-\|^- \[ \] \*\*REPO-\|^- \[ \] \*\*DEF-\|^- \[ \] \*\*GEN-\|^- \[ \] \*\*DOM-\|^- \[ \] \*\*PER-\|^- \[ \] \*\*DDL-\|^- \[ \] \*\*HTTP-\|^- \[ \] \*\*DISC-\|^- \[ \] \*\*LIST-\|^- \[ \] \*\*PATCH-\|^- \[ \] \*\*ETAG-\|^- \[ \] \*\*BULK-\|^- \[ \] \*\*RES-\|^- \[ \] \*\*CLI-\|^- \[ \] \*\*LIB-\|^- \[ \] \*\*OBS-\|^- \[ \] \*\*TEST-\|^- \[ \] \*\*REPO-' .planning/REQUIREMENTS.md` is unchanged from before this plan ran (we did NOT add or remove requirements; the count for v1 should remain 85).

Use the Edit tool for each individual replacement to keep the diff minimal and reviewable.
  </action>
  <verify>
    <automated>! grep -nE 'scimgen|scimrt' .planning/REQUIREMENTS.md && ! grep -nE 'scimgen|scimrt' .planning/ROADMAP.md && grep -q "REQ-REPO-02" .planning/REQUIREMENTS.md && grep -q "github.com/imulab/go-scim/gen" .planning/REQUIREMENTS.md && grep -q "rt/observe" .planning/REQUIREMENTS.md && grep -q "scim CLI built on Cobra\|scim\` CLI" .planning/REQUIREMENTS.md && grep -q "scim CLI" .planning/ROADMAP.md && grep -q "(\`gen\` and \`rt\`)" .planning/ROADMAP.md</automated>
  </verify>
  <done>
Zero occurrences of `scimgen` or `scimrt` remain in REQUIREMENTS.md or ROADMAP.md. REQ-REPO-02 explicitly names `gen` and `rt` with their `github.com/imulab/go-scim/{gen,rt}` paths. REQ-CLI-01..04 use `scim` as the CLI binary name. Phase 0 success criterion 3 in ROADMAP.md says `(\`gen\` and \`rt\`)`. The reconciliation footer line is appended to REQUIREMENTS.md (not ROADMAP.md, where it's not idiomatic). Total v1 requirement count unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Go version floor in .planning/research/STACK.md</name>
  <files>.planning/research/STACK.md</files>
  <action>
Update `.planning/research/STACK.md` to reflect the Go floor override (1.24 → 1.25). Use the Edit tool for each replacement.

Targeted replacements (line numbers approximate; use the exact match strings shown):

1. Replace the table row `| Go toolchain | **Go 1.24+** (use 1.25 if available) | Tool directives in \`go.mod\`, generic type aliases, Swiss Tables runtime |` with `| Go toolchain | **Go 1.25+** (CI tests 1.25 + 1.26) | Tool directives in \`go.mod\`, generic type aliases, Swiss Tables runtime, \`go.work\` workspaces |`.

2. Replace `| Go | 1.24.x (1.25 once available) | Toolchain | Tool directives in \`go.mod\` (replaces the \`tools.go\` hack), generic type aliases land fully, Swiss Tables runtime gives 2-3% CPU back, \`synctest\` graduates to GA. Released Feb 2025; current patch 1.24.13 (Feb 2026). [Confidence: HIGH] |` with `| Go | 1.25.x floor; CI matrix 1.25 + 1.26 | Toolchain | Tool directives in \`go.mod\`, generic type aliases, Swiss Tables runtime, \`go.work\` workspaces. Go 1.25 floor per Phase 0 CONTEXT.md decision; user runs 1.26.1. [Confidence: HIGH] |`.

3. Replace `| Go | 1.24+ | Same as generator | Generated code targets the same minimum |` with `| Go | 1.25+ | Same as generator | Generated code targets the same minimum |`.

4. Replace the in-code-example line `go 1.24` (around line 194) with `go 1.25`. If there are MULTIPLE `go 1.24` literals inside code fences (representing example go.mod files for both gen and rt modules), update each. Do NOT change historical version commentary like "Released Feb 2025" or "current patch 1.24.13 (Feb 2026)" — those are factual statements about Go's release history, not project requirements.

5. Replace any remaining `Go 1.24+` literal with `Go 1.25+` in the same surgical pattern. Run `grep -n "1\\.24" .planning/research/STACK.md` after edits — the ONLY allowed remaining matches are historical references inside parenthetical commentary (release date / current patch version statements). If any line still implies 1.24 IS the project floor, update it.

6. Append at the end of the file (after the existing footer if any, otherwise as a new italic last line):
   `*Updated 2026-05-07 (Phase 0 plan) — Go floor bumped 1.24 → 1.25 per CONTEXT.md decision. CI matrix tests 1.25 + 1.26.*`

After edits, run `grep -nE '\\b1\\.24\\b' .planning/research/STACK.md` and confirm any remaining matches are historical (release dates, version commentary), NOT project-floor declarations.

Do NOT update other research docs (.planning/research/ARCHITECTURE.md, FEATURES.md, PITFALLS.md, SUMMARY.md). The override is to STACK.md only because STACK.md is where the version floor is specified. If those other research docs mention 1.24 in passing, they'll be reconciled organically when their phases are planned. Surgical: STACK.md only in this plan.
  </action>
  <verify>
    <automated>grep -q "Go 1.25" .planning/research/STACK.md && grep -q "1.25 + 1.26\|1.25 \\+ 1.26" .planning/research/STACK.md && grep -q "go 1.25" .planning/research/STACK.md && grep -q "Phase 0" .planning/research/STACK.md && grep -q "Updated 2026-05-07" .planning/research/STACK.md</automated>
  </verify>
  <done>
.planning/research/STACK.md states Go 1.25 as the project floor (table rows + code examples), notes the 1.25 + 1.26 CI matrix, and includes a footer line citing the Phase 0 CONTEXT.md decision and the date 2026-05-07. Any remaining `1.24` references are historical (Go release dates / patch version commentary), not project-floor declarations. The other research docs are untouched.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `grep -nE 'scimgen|scimrt' .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns ZERO lines.
2. `grep -nE 'scim CLI|\\\`scim\\\` CLI' .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns AT LEAST 4 lines (REQ-CLI-01..04 mentions + ROADMAP Phase 7 mentions).
3. `grep -nE 'github.com/imulab/go-scim/(gen|rt)' .planning/REQUIREMENTS.md` returns AT LEAST 1 line (REQ-REPO-02 fully qualified).
4. `grep -n "Go 1.25" .planning/research/STACK.md` returns AT LEAST 3 lines (project-floor table rows updated).
5. `grep -nE '\\bgo 1\\.24\\b' .planning/research/STACK.md` returns ZERO lines for the project-floor declarations (any 1.24 mentions remaining MUST be in parenthetical historical commentary).
6. Total v1 requirement count in REQUIREMENTS.md is UNCHANGED at 85 (Plan 04 reconciles wording, never adds or removes requirements).
7. Both files have a footer note citing the Phase 0 plan / 2026-05-07 / CONTEXT.md so a future reader sees the audit trail.
</verification>

<success_criteria>
- All `scimgen` / `scimrt` references in REQUIREMENTS.md and ROADMAP.md replaced with `gen` / `rt` / `scim` per the override mapping in CONTEXT.md `<requirements_overrides>`.
- Go version floor in .planning/research/STACK.md raised from 1.24 to 1.25 with a note explaining the override.
- Footer/inline reconciliation notes on edited files cite the Phase 0 plan and 2026-05-07 so the override trail is preserved.
- No new requirements added; no existing requirement IDs renamed; total v1 count remains 85.
- Plans 01, 02, 03 (which already use `gen`/`rt`/`scim` verbatim) and the future Phase 1+ planners now read consistent docs.
</success_criteria>

<output>
After completion, create `.planning/phases/00-architecture-spike-repo-setup/00-04-SUMMARY.md` documenting:
- Exact edits applied (file:line, before/after) — useful when checker validates Plan 04.
- Confirmation that requirement count is unchanged (`grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` = 85).
- Any new `1.24` occurrences left in STACK.md (and confirmation they are historical, not project-floor).
- Forward note: when planning Phase 7 (CLI), planner should reread REQUIREMENTS.md to absorb the `scim` binary name and `gen`-as-library shape decided here.
</output>
